version: '{build}'
image: Visual Studio 2017
platform:
  - Win32
  - x64
configuration:
  - Release
build:
  parallel: true
  project: AvsDec.sln
  verbosity: minimal
artifacts:
  - path: src\bin\$(configuration)\$(platform)
    name: AvsDec-$(platform)-$(configuration)-build$(appveyor_build_version)
